# PostgreSQL Configuration
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin_password
POSTGRES_DB=auth_db

# MongoDB Configuration
MONGO_USER=admin
MONGO_PASSWORD=admin_password
MONGO_DB=chat_db

# Redis Configuration
# (Redis alpine image doesn't require env vars for default auth-less setup,
# but we can add requirepass if needed. Keeping simple for dev.)